# steps to run:

steps:
  - npm init -y
  - npm install --save-dev typescript
  - npx tsc --init
  - npm init -y
  - npm install --save-dev typescript
  - npx tsc --init
  # compile the TypeScript files
  - npx tsc
  # run the example file
  - node dist/example.js



